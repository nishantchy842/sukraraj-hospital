export const metadata = {
   title: 'Client Concern Categories | Admin Panel',
};

type Props = {
   children: React.ReactNode;
};

export default function RootLayout({ children }: Props) {
   return children;
}
