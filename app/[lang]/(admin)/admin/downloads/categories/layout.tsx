export const metadata = {
   title: 'Downloads Categories | Admin Panel',
};

type Props = {
   children: React.ReactNode;
};

export default function RootLayout({ children }: Props) {
   return children;
}
